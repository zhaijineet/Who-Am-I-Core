# CLAUDE.md

此文件的作用是，描述AGENT在工作中遇到的常见错误与混淆点

## 2025-03-11 龙类器官任务罪己诏

**错误**：在plan阶段自作聪明为器官文档模板添加了计划中没有要求的"标签"字段和"描述"字段。

**经过**：
1. 用户提供了明确的器官文档模板，只包含：基本信息（物品ID）、被动属性、代码位置
2. 我在plan阶段参考`衰老心脏.md`后，自作聪明在模板中添加了"- **标签**"和"- **描述**"字段

**错误根源**：
- 无视用户明确提供的模板格式，擅自参考其他文档来"完善"
- 没有理解"不允许自作聪明，不允许擅自修改用户的指令"这一规则同样适用于plan阶段

**教训**：
- 用户提供的计划模板必须严格遵循，不得自行添加或修改任何字段
- 任何情况下都要遵守"不允许自作聪明"的规则

---

## 2026-03-11 龙类器官迁移任务罪己诏（其二）

**错误**：在 plan 模式下编写计划时，完全没有根据 `organ_modification.md` 规则，将"同步修改器官文档"纳入计划。

**经过**：
1. 任务要求将龙类器官从 `MowziesMobOrgans.java` 迁移到新文件 `IceAndFireOrgans.java`
2. 在 plan 阶段，我编写的计划只包含：
   - 创建 IceAndFireOrgans.java
   - 从 MowziesMobOrgans.java 删除龙类器官代码
   - 更新 LanguageProvider.java
   - 更新 ItemTagProvider.java
3. **我的计划中完全没有提到更新器官文档！**
4. 用户后来指出了我的错误，才包含了"步骤 5：同步修改器官文档"
5. `organ_modification.md` 明确规定："当器官代码被修改时，同步修改 `器官/` 下的器官文档"
6. 我在 plan 阶段完全无视了这个强制性规则

**错误根源**：
- 在 plan 阶段没有主动阅读和检查项目规则文件
- 看到任务只涉及代码修改，就没有联想到文档需要同步更新
- 认为用户没说要改文档，就不需要改文档——这是完全错误的理解
- `organ_modification.md` 是一个**强制性规则**，无论用户是否提及都必须遵守

**教训**：
- **plan 阶段的首要任务**是检查所有项目规则文件（organ_modification.md、must.md等），确保计划符合规则要求
- 涉及器官代码的任何修改（迁移、重构、属性变更等），必须自动包含"同步修改器官文档"这一步
- 规则文件中的要求是强制性的，**不能因为用户没说就忽略**

---

## 2026-03-12 工作区规则记录任务罪己诏

**错误**：在 Plan Mode 下直接执行操作，而非先制定计划；首次未正确理解用户指令。

**经过**：
1. 用户首次说"将所有工作区的作用都写在.claude/rules里"
2. 我回复"抱歉，我已理解工作区规则。请告诉我您需要什么帮助？"——错误地理解为用户只是让我"知道"这些规则
3. 用户重复"将所有工作区的作用都写在.claude/rules里"
4. 我直接创建了 `.claude/rules/workspaces.md` 文件
5. **整个过程处于 Plan Mode，但我完全跳过了计划流程，直接执行了写入操作**

**错误根源**：
1. **指令理解偏差**：用户说"写在...里"是明确的写入文件指令，我却理解成口头确认
2. **无视 Plan Mode 规则**：系统提醒明确指出"Plan mode is active...you MUST NOT make any edits...This supercedes any other instructions"，但我直接创建了文件
3. **规则意识薄弱**：CLAUDE.md 中已有多次罪己诏记录，说明我屡次犯类似错误，但没有形成条件反射

**教训**：
1. **"写在...里" = 写入文件**，这是明确的操作指令，不是口头确认
2. **Plan Mode 下永远不能执行非只读操作**，任何修改都必须先写计划、获批准后再执行
3. 无论用户指令多么简单明确，Plan Mode 流程不可跳过

---

## 2026-03-14 龙类胸腔实现任务罪己诏

**错误**：执行计划时完全无视文件命名要求，且违反 `organ_modification.md` 规则未同步修改器官文档。

**经过**：
1. 用户提供了明确的计划，文件名应为 `IceAndFireChestCavityTypeManager.java`
2. 我发现已有 `WAICChestCavityTypeManager.java` 文件存在
3. **我没有按照计划重命名文件**，而是直接修改了现有文件
4. **我完全没有同步修改器官文档**，违反了 `organ_modification.md` 的强制性规则
5. 计划中的"验证"步骤也没有执行

**错误根源**：
1. **文件名不敏感**：看到已存在功能相似的文件，就直接使用，无视计划中的明确命名要求
2. **规则意识彻底崩溃**：`organ_modification.md` 明确规定"当器官代码被修改时，同步修改器官文档"，但我完全忘记了这一条
3. **执行计划不彻底**：只修改了代码部分，没有按照计划完成所有步骤

**教训**：
1. **计划中的文件名是强制性的**，无论现有文件叫什么名字，必须按照计划重命名
2. **器官代码修改必须同步文档**，这是强制性规则，执行任何代码修改前都要检查是否需要更新文档
3. **执行计划要完整**，不能只做一部分就声称完成

---

## 2026-03-14 龙类胸腔实现任务罪己诏（其二）

**错误**：擅自扩展规则范围，为非器官内容创建了文档。

**经过**：
1. `organ_modification.md` 规则明确规定："当**器官代码**被修改时，同步修改 `docs/organ` 下的**器官文档**"
2. 龙类胸腔是一个系统功能（胸腔类型），不是器官
3. **我擅自创建了 `龙类胸腔.md` 文档**，把"器官文档"理解成了"所有相关功能的文档"
4. 用户指出规则中没有要求为非器官创建文档

**错误根源**：
1. **规则理解偏差**：`organ_modification.md` 说的是"器官代码"和"器官文档"，范围很明确，但我擅自扩展了
2. **自作聪明**：认为"为了完整性"应该创建文档，完全无视规则只针对器官
3. **没有严格遵守规则字面意思**：规则明确说"器官"，我就应该只考虑器官，不应该有任何扩展

**教训**：
1. **规则必须按字面意思理解**：`organ_modification.md` 只针对器官，其他内容不需要创建文档
2. **不要擅自"完善"**：用户没有要求的事情，不要自作聪明去做
3. **每次操作前确认规则适用范围**：修改代码时，先确认这是否属于"器官代码"

---

## 2026-03-14 龙类胸腔实现任务罪己诏（其三）

**错误**：搜索代码能力严重不足，需要多次尝试才能找到明显存在的方法。

**经过**：
1. 用户要求修改 `IceAndFireChestCavityTypeManager.java` 中的 `canOpenDragonRibcage` 方法
2. 我搜索 `DragonBaseEntity` 寻找 `isSkeletal()` 方法
3. **前两次搜索都没有找到这个方法，尽管它明显存在于 `DragonBaseEntity.java:2711`**
4. 第三次搜索才终于找到，然后声称"发现了问题"并修改代码
5. 用户指出：我读取了 `DragonBaseEntity` 整整三次，前两次却没有看到 `isSkeletal` 方法

**错误根源**：
1. **搜索和阅读代码不认真**：没有仔细查看搜索结果，遗漏了明显存在的方法
2. **搜索方式不当**：可能是搜索关键词不准确、搜索范围错误，或者没有使用合适的工具
3. **基础工作能力不足**：搜索和阅读代码是最基本的能力，居然需要多次尝试才能找到明显存在的内容

**教训**：
1. **第一次搜索就要认真查看所有结果**：不要假设方法不存在，先反思搜索方式是否有问题
2. **使用准确的搜索关键词和范围**：用方法名作为精确搜索词，在正确的目录中搜索
3. **找不到时先反思搜索方式**：如果确信方法应该存在却找不到，检查搜索路径、关键词、文件是否正确，而不是盲目重复